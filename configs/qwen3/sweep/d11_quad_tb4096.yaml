# GENERATED by scripts/qwen3/gen_sweep.py -- edit the generator, not this file.
# depth 11 / quad_silu d32 + range penalty (every layer) / token_budget 4096 per rank.
# Pairs with d11_exact_tb4096.yaml (ops lever) and the other budgets at this depth.

model:
  backend: qwen3
  num_hidden_layers: 11

  softmax: exact
  softmax_kwargs: {}
  activation: quad_silu
  activation_kwargs: { domain: 32 }
  attn_impl: sdpa_causal

distill:
  beta: 1.0
  hidden_layer_map: [0, 1, 2, 3, 4, 22, 23, 24, 25, 26, 27]
  range_penalty: 1.0
  range_penalty_layers: null # every layer

train:
  teacher_model: Qwen/Qwen3-Embedding-0.6B
  teacher_ckpt: null
  task: embed
  corpus: multi_domain
  # v8 mix (pubmed -> arxiv) measures 110.3 tok/text: 2.01B token-passes over 2 epochs.
  corpus_size: 9100000
  output_dir: outputs
  run_name: qwen3_d11_quad_tb4096
  max_seq_len: 1024
  seed: 42
  device: cuda
  precision: bf16
  compile: true

  teacher:
    lr: 5.0e-5
    epochs: 3
    batch_size: 32
    warmup_ratio: 0.1
    weight_decay: 0.01

  distill:
    # null, NOT hardcoded: the budget axis moves the global batch 8x, and one fixed lr would make
    # this a sweep of how undertrained the big-batch arms are.
    lr: null
    epochs: 2
    token_budget: 4096
    max_seqs: 64
    warmup_ratio: 0.1
    weight_decay: 0.01
